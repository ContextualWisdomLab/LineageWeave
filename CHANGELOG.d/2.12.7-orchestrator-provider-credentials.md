### Fixed

- Register every available canonical provider credential from the private
  Compose environment with contextual-orchestrator auto-discovery instead of
  collapsing provider identities into one gateway key.
- Let contextual-orchestrator select the embedding-capable model and persist
  its returned identity instead of configuring a provider model locally.
- Reuse fast-mlsirm's Rust-backed GRM/GPCM prediction API for report residuals
  instead of duplicating item-parameter arithmetic in LineageWeave.
