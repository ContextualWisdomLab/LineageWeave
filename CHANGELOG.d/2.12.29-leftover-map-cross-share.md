## 2.12.29 — Leftover-map cross share

- Persist leftover-map cross share `x = 2 R̂_c U_c / R̃²` of centered
  leftover on leftover post–criterion pairs (ADR 0185). After
  `make seed`, closest and farthest leftover pairs sit above the
  member list with `2R̂U/R̃²` next to leftover-map distance `d`; click
  opens that post. Omit the badge when the share is missing. A signed
  remainder is shown, never clamped. Never invent a leftover score. Do
  not persist leftover-map explained leftover share `e`, unexplained
  leftover share `s`, unexplained leftover `U`, or reconstruction `R̂`.
