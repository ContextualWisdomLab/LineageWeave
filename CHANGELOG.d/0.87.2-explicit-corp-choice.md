# 0.87.2 Explicit corp choice before lineage create

Request stays disabled until a multi-affiliation operator picks a
corp. An account with no affiliated corp sees Ask an administrator
to affiliate this account and cannot POST. Preview the empty choice
in Storybook (`Analysis/LineageEntityPicker` / `NeedsExplicitChoice`).
