# 2.20.0 Project-history response contract

Project-history storage projections now emit the same strict evidence-bound
shape that the HTTP response validates. Project identity display names retain
their authoritative code/key, event source IDs and time basis are explicit,
and the endpoint no longer fails with a validation error for an authorized
timeline.
