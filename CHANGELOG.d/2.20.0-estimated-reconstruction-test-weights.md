### Changed

- Reconstruction, persistence, seed, graph-separation, and HTTP acceptance tests now share one Rust-backed fast-mlsirm fixture estimate instead of hand-authored fusion weights; the LLM wiring check uses an explicit zero-weight ablation.
