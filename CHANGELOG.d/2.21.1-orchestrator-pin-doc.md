# 2.21.1 — Align the orchestrator runtime pin record

## Fixed

- Align ADR 0083 with the immutable contextual-orchestrator commit used by the
  Compose image and add a regression check so the image and ADR cannot drift.
