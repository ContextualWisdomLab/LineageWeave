## Added

- Persisted Event Lineage temporal, secondary-key, text, and available LLM channel scores as normalized child evidence for each selected edge.
- Added a Buyer-facing exact-value evidence table and matching SVG descriptions so customers can review why two posts were linked before relying on the reconstructed connection.

## Changed

- Missing reconstruction channels are now shown as unavailable rather than fabricated as zero.
- The Event Lineage API now keeps fused ranking and channel-level evidence distinct; neither is promoted to a causal or authoritative fact.
