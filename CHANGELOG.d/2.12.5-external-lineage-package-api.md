## Added

- Export the versioned external lineage request/result types, strict parser, canonical serializers and digests, stable contract error, and store-agnostic `analyze_external_lineage` entry point from the public `lineageweave` package.
