# 2.20.1 — Provider temperature capability negotiation

- Compose now pins ContextualWisdomLab/contextual-orchestrator#791 so providers
  that accept only their default sampling temperature can retry the same
  request without the unsupported optional field, alongside the orchestrator's
  embedding-capability selection contract.
- The pin remains an immutable upstream commit and does not copy provider
  behavior or credentials into LineageWeave.
