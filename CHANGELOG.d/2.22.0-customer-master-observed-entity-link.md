### Added

- Customer Master now surfaces a verified ancestor entity even when it was
  never itself mentioned in any post an account can read -- only inferred
  as a parent from another account's private evidence (ADR 0144). A new
  write-time `account_observed_entity` link, populated at the same three
  `get_or_create_corporate_entity` ingestion sites, records the account
  set already authorized to read the source post; a bounded recursive
  read-time walk over `corporate_entity.parent_entity_id` then admits the
  whole verified chain, tagged `observed_hierarchy`. Revocation is live
  (a read-time join back to `account_affiliation`, no reconciliation lag);
  a new grant widens on the next observation event or the nightly
  `scripts/reconcile_observed_entities.py` backstop.
