# 2.20.2

- Delegate embedding-model selection to contextual-orchestrator, retain the
  resolved model as database provenance, and keep provider model selectors out
  of the LineageWeave backend process.
- Discard the legacy embedding selector at the Compose bootstrap so automatic
  capability discovery remains the only model-selection path.
- Keep the backward-compatible embedding client name aligned with the
  orchestrator client's batch and per-request provenance contract.
