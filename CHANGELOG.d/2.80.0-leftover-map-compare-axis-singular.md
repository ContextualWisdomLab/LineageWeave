### Added

- Grouping comparison leftover-axis evidence now names persisted Gabriel singular values independently of persisted axis share. Combined, singular-only, share-only, and empty states remain distinct; finite `σ=0` is preserved and neither measure is derived from the other.
