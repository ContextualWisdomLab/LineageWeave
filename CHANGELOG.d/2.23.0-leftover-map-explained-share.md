## 2.23.0 — Leftover-map explained leftover share

- Persist leftover-map explained leftover share `e = R̂² / R²` of raw
  residual on leftover post–criterion pairs (ADR 0266). After
  `make seed`, closest and farthest leftover pairs sit above the
  member list with `R̂²/R²` next to leftover-map distance `d`; click
  opens that post. Omit the badge when the share is missing. A share
  greater than 1 is shown, never clamped. When `R`, `R̂`, `U`, `x`,
  `s`, and `e` are finite, `e + s + x = 1`. Never invent a leftover
  score. Never invent a theta.
