# Restore login typecheck without unauthenticated AdminPanel

- Login uses `returnUrlFromLocation` and `rememberOidcReturnUrl`.
- The unauthenticated shell does not mount Admin settings, so `tsc -b`
  no longer fails on an undefined access token.
- App tests assert that login does not render admin settings and that
  both OIDC storage surfaces receive the return URL.
- Does not rewrite `docs/product-technical-gap-baseline.md`; that file
  stays on LineageWeave#426 (ADR 0001).
