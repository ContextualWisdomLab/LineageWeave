# Product & Technical Gap Baseline

> Current authority snapshot: 2026-09-16.
>
> Protected `main` observed at `83eba56149eb802cd63642c507c324c9976ec78e`.
> Protected refs, live PRs/Issues, ADRs, exact heads and exact-head receipts are authoritative.
> Historical overlays through 2026-09-13 remain in
> [`docs/evidence/product-technical-gap-baseline-history-through-20260913.md`](evidence/product-technical-gap-baseline-history-through-20260913.md).

## Protected delivery baseline

No LineageWeave release is admitted from the current protected head. A moved parent invalidates descendant acceptance evidence. A semantic/non-force convergence preserves valid deltas but never transfers predecessor validation receipts.

### Summary shared-catalog authorization

#1079 remains the Customer Master/shared-catalog authorization candidate at exact `c2923950e73c88a9f9fd932332ddd47682da124b`. Product Tests/SAST/Security are terminal GREEN, but required CodeQL is legitimately RED. Canonical producer run `34922377994` completed all actual scans: JavaScript/TypeScript and Python analyses succeeded, then the Medium+ SARIF gates failed; Actions produced no retained product finding. The findings are repository-baseline files outside #1079's 13-file authorization delta, so they are owner work rather than new summary-catalog authorization regressions. Python findings are owned by #974; frontend parser findings are owned by #983, now exact `63a72ef89f5a97aef1e0139430ee659efd8d9478`, whose deterministic parser repairs are present but remain unaccepted. Authority correction is recorded on #1079 as comments `5689248387` and `5689804039`. Authenticated OpenCode verdict, complete Strix/Noema owner-path settlement and qualifying independent approval also remain outstanding. Canonical review/runtime queue, provider, timeout, retry and credential behavior stays in `.github` and contextual-orchestrator; LineageWeave does not copy it.

### Commercial-safe synchronous PostgreSQL boundary

#911 remains exact `6030b295aadc3ee76dc4d27f5713273f35888325`. Its test-first pg8000 compatibility repair preserves libpq-style integer `connect_timeout` semantics and treats non-positive values as no-deadline sentinels. Runtime persistence remains `asyncpg`. Exact-head Tests `34978833151`, PROV-O `34978832987`, Ontology Pages `34978833063`, and SAST `34977841093` are terminal GREEN. Security `34977841173` is terminal `failure`: changed-scope, OSV, Trivy and Scorecard completed successfully, while dependency-review job `104497459473` passed exact checkout/verification, failed `Check dependency review support`, and skipped the pinned Dependency Review action. That is the intended fail-closed shape of the still-open canonical `.github#810` availability/configuration incident, not evidence of a new pg8000/libpq source regression. Fresh downstream owner evidence is recorded in `.github#810` comment `5689139333`. CodeQL PR `34977841115` is also terminal `failure` at the canonical producer/compatibility settlement boundary rather than an accepted source-analysis GREEN, and there is still no qualifying independent exact-head `APPROVED` review. Ready state is validation admission, not merge acceptance.

### Owner-boundary and governed measurement stack

#899 is the contextual-orchestrator consumer-boundary foundation at exact `a2da5875525cd0950999487ff8fe7d439284dbd2`. Review of the live Draft found ADR 0300 prematurely marked `Accepted` before the decision had landed on the protected integration path. Minimal causal repair changes only that lifecycle marker to `Proposed`; the provider/model/routing ownership decision itself is unchanged. Exact-head Tests `35076460845` are terminal `skipped` under Draft admission, while SAST `35076460791`, Security `35076460767`, and CodeQL PR `35076460779` remain queued. None is merge acceptance. Descendant evaluation/measurement lanes remain separate from LineageWeave domain truth; provider/model/routing behavior stays in contextual-orchestrator and psychometric truth stays in its canonical owners.

#902 is exact `9487e0299a148a10bfcf7b02110508078843d3ef` directly on #899 `a2da5875525cd0950999487ff8fe7d439284dbd2`. Ordinary two-parent convergence preserves the dichotomous measurement-policy delta and adopts the exact Proposed ADR 0300 blob; fresh compare has merge-base exactly at #899 and `behind_by=0`. ADR 0301 remains `Proposed`. Exact-head Tests `35076625189` are Draft-skipped and are not GREEN. LineageWeave owns governed rubric/version/lifecycle/activation and observation-policy vocabulary; numerical IRT kernels remain in fast-mlsirm, temporal/event/multilevel measurement semantics in TEPP, and provider/judge execution in contextual-orchestrator.

#915 is exact `e2a0038382c7fb4f75f66cad473afef4b9a8353a` directly on #902 `9487e0299a148a10bfcf7b02110508078843d3ef`. Ordinary two-parent convergence preserves all dynamic-evaluation source/tests/docs while adopting the moved parent exactly; fresh compare has merge-base exactly at #902 and `behind_by=0`. Exact-head Tests `35076662215` are Draft-skipped and predecessor receipts do not transfer. Its projection keeps criterion, observation, adjudication, calibration, promotion, linking and supersession provenance distinct without absorbing contextual-orchestrator execution, fast-mlsirm psychometrics, or TEPP temporal truth.

#966 is Draft at exact `f93715af6cbab2a39c22d7c170df2dc32bcd7456` directly on #899 `a2da5875525cd0950999487ff8fe7d439284dbd2`. Ordinary two-parent convergence preserves the semantic operator/API naming tree and adopts the Proposed ADR 0300 blob; fresh compare has merge-base exactly at #899 and `behind_by=0`. Exact-head Tests `35076707884` are Draft-skipped and no open PR currently targets this branch. Historical opening-body snapshots do not override the current Git refs or authority comment `5694854368`.

#919 is Draft at exact `d78390c56c9d9abcf4a97d2ffa5477704dcbb465` directly on #899 `a2da5875525cd0950999487ff8fe7d439284dbd2`. Ordinary two-parent convergence preserves its bounded Keyman/channel-weight/thread-group/Vision operator delta and adopts the Proposed ADR 0300 blob; fresh compare has merge-base exactly at #899 and `behind_by=0`. Exact-head Tests `35076891083` are Draft-skipped and there are no open descendants targeting this branch. No provider credential, endpoint, model-selection or routing authority is moved into LineageWeave.

### CodeQL baseline security ownership

Canonical producer `34922377994` preserved three immutable SARIF artifacts. Python artifact `10398012754` exposed `py/insecure-protocol` in `lineageweave/http_client.py` and `py/polynomial-redos` in `lineageweave/post_chat.py`; both files belong to active #974 rather than #1079. #974 carried the TLS finding through executable RED `9177894c19339de137aed5fb71dcad9c6a725ec2` and causal fix `0ac8bd5dd85321d84d69782f72aab56e47e33b8b`, explicitly setting a TLS 1.2 minimum while retaining certifi chain and hostname verification. It then carried the user-input regex finding through RED `216e4e3c41161f49b65c48128e8c178790222e3d` and causal fix `6911954363888d1e2d523ebbcf68f68d7217752a`, replacing the end-anchored punctuation regex with linear whitespace normalization plus `.rstrip("?.!")`. These are source-level causal repairs. #974 is Ready for validation admission only: Tests `35033878120`, SAST `35033619322`, and Security `35033619472` are now terminal GREEN on exact `69119543...`; CodeQL PR `35033619584` remains queued. Earlier Tests `35033619342` was a Draft-event skip and is not acceptance, and no qualifying independent exact-head `APPROVED` review exists.

#979 was immediately non-force converged after each #974 movement and is now exact `2431fa8cde5927ca9e6afeb2652ff8b01f3f0a85` directly on #974 `6911954363888d1e2d523ebbcf68f68d7217752a`. Its claim-generation/queue/schema delta remains distinct, the parent security files are adopted unchanged, and predecessor receipts do not transfer. There are no open descendants below #979 at this snapshot.

JavaScript artifact `10398331800` exposed four `js/incomplete-multi-character-sanitization` findings in `frontend/src/postBodyDisplay.ts`. #983 owns that parser/rendering boundary. RED `ec126f7babb73269c0d4cbf0e7b9a7b4c8ca0a80` reproduced tag recomposition and nested quoted-`>` truncation; causal fix `a91e3724978e95f0902b3e77df91f50f7baf3599` replaced the sanitizer-style tag-removal rewrites with a deterministic HTML-like scanner. Outer `sup/sub` attributes containing quoted `>` were then fixed test-first through RED `3df683d4a0874aa80bce9aefeb36ac2eb69d995e`, causal refactor `a8b3f3b017d8bde3f1578c57025dd918a003b76d`, and non-destructive TypeScript typing follow-up `7ff923792b18f8bb906a68654e31f3cf6f6b209c`. The embedded-image thread next exposed `<img>`/quoted-`src` parsing; predecessor `611e56a6d42b38542265fb0fc080c10ae9f454a7` introduced the quote-aware image boundary that retains only validated `data:image/*;base64` while remote image URLs stay on the stripping path. A further structural-boundary RED `43d64df2f07b05f58292c4d69641c15973238525` showed break/block/OOXML-indent handling could still terminate tags at quoted `>`; current #983 exact `63a72ef89f5a97aef1e0139430ee659efd8d9478` is the causal repair, routing structural boundaries through the same quote-aware scanner while preserving list depth and declared indentation semantics.

#983 remains validation-only at exact `63a72ef89f5a97aef1e0139430ee659efd8d9478`. Draft-event Tests `35050351017` are terminal `skipped` and are not GREEN. Fresh exact-head Tests `35050376521`, SAST `35050351026`, Security `35050351037`, and CodeQL PR `35050351127` are queued and are not acceptance evidence. Predecessor checks and status contexts do not transfer or substitute for a qualifying independent exact-head `APPROVED` review. Parent movement was converged immediately without force: #984 is `84c644bd3876ca642358698bbb2ec89d51330510`, #992 is `0a3e166fffdcb1626089f79f66f0256fb778fc8b`, and sibling #985 is `d7ffc10c...`. Fresh #984→#992 comparison has merge-base exactly at #984 and `behind_by=0`; #992 effective child delta remains only the gap baseline and documentation-hygiene test. No predecessor acceptance receipt transfers.

### Backend embedded-image structural extraction

#1115 is the LineageWeave-owned ingestion-side counterpart to #983's frontend display repair. Protected-main `_DATA_URI_IMG` used a tag-wide `[^>]*` expression, so a valid quoted `>` before `src` caused a data-URI image to disappear from `extract_base64_images` even though the frontend display path could retain it. RED `2b50f8014c147d980a923a14e41f36d7e2377b4b` fixes that behavior as an executable contract; causal source repair `5de750d7d55dc78dee6257ef91215b8ac37679f1` replaces tag-wide parsing with Python `HTMLParser` attribute handling plus the same narrow `data:image/*;base64` value allowlist. `41f28f1605d4c93914b146dbf60c7b35f3348814` adds ordinary LF-separated multiline character-offset coverage, and `1191142dbe7b73ed3407617c8381324f3bec1493` closes parser edge branches for case-insensitive/self-closing tags, absent or valueless `src`, remote-image rejection and strict-decode failure. Remote HTTP(S) images remain ignored and contextual-orchestrator/provider ownership is untouched.

Current-head review then exposed a separate provenance defect in the new offset adapter: `HTMLParser.getpos()` advances line identity only on actual LF (`\n`), while the predecessor `str.splitlines()` table also treated `\r`, `\v`, and other separators as line breaks. RED `b2e29b6ade8fb651e27fa50d5580204bd641037c` fixes `"\rtext\n<img ...>"` as an executable contract requiring `EmbeddedImage.position == html.index("<img")`; minimal causal fix `6545b5ff7ed88d98daad74ca3ba8f8606dad3fc4` now builds source line starts only from actual LF positions with `re.finditer("\n", html)`, matching `HTMLParser.getpos()` semantics. The CodeRabbit thread is resolved/outdated after this RED→fix pair.

#1115 is Ready for validation admission only at exact `6545b5ff7ed88d98daad74ca3ba8f8606dad3fc4`, directly on protected `main@83eba56149eb802cd63642c507c324c9976ec78e`, with merge-base exact and no descendant. Fresh exact-head Tests `35065642195`, Security `35065642147`, SAST `35065642221`, and CodeQL PR `35065642188` remain queued. Tests jobs `104695187887` and `104695188053` are both pre-runner at `runner_id=0`, `steps=[]`, label `ubuntu-latest`; CodeQL detect job `104695188093` is likewise pre-runner at `runner_id=0`, `steps=[]`, but label `ubuntu-24.04`. The only submitted review is CodeRabbit `COMMENTED` on predecessor `1191142d...`; there is no qualifying independent current-head `APPROVED` review. No predecessor/local receipt substitutes for exact-head acceptance.

## Active leftover-map foundation finding

#866 is now exact `6935ac4d3850ae24d23fab76be70fe22102ef4ee` on #865 `0728f56ba66f16783685c84d9d3aa034eea9f143`. The prior buyer-path RED was valid: `frontend/src/App.tsx` bypassed the four-state `leftoverMapAxisBadge(axis)` projection and directly composed the primitive singular/share helpers, allowing a badge shell when both persisted evidence values were unusable. Current source contains the minimum causal product repair: `App.tsx` now consumes `leftoverMapAxisBadge(axis)`, omits the badge when the projection is null, and otherwise renders the returned template/values. No SQL, persistence, estimator or canonical-owner contract changed.

That source repair is not accepted GREEN. Exact-head Tests run `35049868360` completed `action_required` with no jobs materialized; pending deployments are empty. It therefore provides no repository test receipt, and no predecessor evidence transfers. The report stack remains Draft until fresh exact-head repository/frontend, rendered a11y/i18n, Security/SAST/CodeQL and qualifying independent review settle.

## Current non-force ancestry

The #866 product-head movement was propagated immediately without force. Current live descendant chain is:

`#867 09ee432b... -> #868 fbd637f1... -> #869 0e57921f... -> #870 43260094... -> #871 3427b5e1... -> #872 02bb2393... -> #873 36ed7500... -> #874 a3eff4a5... -> #875 18448d39...`

Below #875, current exact topology is:

- #876 `6ef9d151...`: report criterion `ζ`, persisted finite item-axis pair only.
- #1033 `86ea83ba...`: comparison criterion `ζ`, same item-coordinate boundary with distinct comparison identity.
- #1034 `bca51350...`: comparison post `ξ`, persisted finite person-axis pair only.
- sibling #877 `ddaa592ab2b3bbd6a50d267fee3e794f0925448e`: canonical formatted-zero origin identity with independent share/σ composition, plus the current comparison-tick i18n RED.

#877 first exposed executable-contract drift during earlier convergence. Production already routed comparison tick accessibility copy through `leftoverMapPlotTickText` and `leftoverMapComparePlotTickAxisBadge`, while `tests/test_grouping_comparison_graphic_tick_contract.py` still required an obsolete inline ternary. That contract was repaired at `8503b0f03d54ce0995e20e9c0149f097b5240e57`; temporary convergence PR #1114 was normally merged. The current `ddaa592a...` head is an ordinary two-parent convergence onto #875 `18448d39...`, preserving the origin/share/σ semantics and the i18n RED without inheriting predecessor receipts.

The still-valid buyer-visible defect is now narrowly scoped. Eight comparison regular/origin tick accessibility templates are passed directly to `tf`, but all eight are absent from each current non-English SPA catalog ko/zh/ja/vi. `tests/test_grouping_comparison_graphic_tick_i18n_contract.py` requires those 32 entries and preservation of `{axis}`, `{value}`, `{share}`, and `{singular}` placeholders. Existing ordinary tick keys are localized, so the missing comparison entries otherwise fall back to untranslated accessibility names.

The existing single-writer one-shot branch was repaired rather than duplicated. Automation commit `3750fc01b8ef9e173915a475f452d4ca6b560b30` retargets both exact-head guards to current #877 `ddaa592a...`; run `35053146771`, job `104657654443`, is queued before any steps have materialized. If admitted, it must observe the executable RED first, insert only the 32 ko/zh/ja/vi static catalog entries, run the tick/origin/i18n focused contracts, exact-head guard the commit/push, and delete its one-shot automation branch on success. Current product Tests `35051594411` are also queued. Neither queued run is GREEN, and the failed predecessor run `35019486346` transfers no repair or acceptance evidence. Broader es/de/fr delivery remains with the governed translation-ledger owner lane.

All current product lanes remain Draft except lanes explicitly promoted only for validation admission. They are converged only to their stated current foundations. No current descendant receipt can be treated as acceptance for a future head.

Historical #878/#879 remain open delta/evidence carriers. Their succession authority is current with #876 `6ef9d151...`, #1033 `86ea83ba...`, and #1034 `bca51350...`; they are not closed merely because reconstructed successors preserve the intended product contracts. Closure still requires complete verified succession of every valid product, test, fixture, contract and evidence delta.

## Buyer-visible gap register

| Gap | Canonical owner / exact candidate | Current evidence | Acceptance still required |
| --- | --- | --- | --- |
| Summary reads must not mutate Customer Master shared catalogs | #1079 `c2923950...` | Product Tests/SAST/Security are GREEN. Canonical CodeQL actual scans are terminal RED on repository-baseline Python/JS findings outside #1079's diff; owner source repairs exist in #974/#983 lanes but are not yet accepted. | Owner repairs integrated/reconverged, fresh exact-head canonical CodeQL GREEN/compatibility settlement, OpenCode/Strix/Noema settlement, qualifying approval, normal protected merge. |
| Commercial-safe synchronous PostgreSQL tooling | #911 `6030b295...` | Exact-head Tests/PROV-O/Ontology/SAST are GREEN. Security is terminal fail-closed because canonical Dependency Review support could not be established; independent Security jobs are green. Required CodeQL is terminal fail-closed at canonical producer/compatibility settlement. | `.github#810` owner repair followed by authoritative exact-head Security GREEN, canonical CodeQL settlement GREEN, and qualifying independent exact-head approval. |
| Contextual-orchestrator ownership decision lifecycle | #899 `a2da5875...` | Draft foundation now carries ADR 0300 as `Proposed`, repairing the premature `Accepted` state without changing the decision text. Tests are Draft-skipped; SAST/Security/CodeQL are queued. Direct descendants #902/#966/#919 and #902 child #915 were immediately converged without force. | Normal protected integration of the owner-boundary decision, fresh required exact-head evidence and qualifying approval; only then may ADR 0300 become `Accepted`. |
| Ask HTTP/chat security baseline | #974 `69119543...` -> #979 `2431fa8c...` | CodeQL-derived TLS and polynomial-ReDoS REDs have test-first causal source fixes; #974 exact-head Tests, SAST and Security are GREEN, while CodeQL remains queued. Child is immediately non-force converged and inherits no acceptance receipt. | Fresh exact-head CodeQL GREEN, provider-boundary regression evidence and qualifying approval before parent-first integration. |
| Post-body parser hardening | #983 `63a72ef8...` | CodeQL findings plus outer script, embedded-image and structural-boundary quoted-`>` cases have deterministic scanner repairs. #984 `84c644bd...`, #992 `0a3e166f...`, and #985 `d7ffc10c...` are current descendants. Fresh Tests/SAST/Security/CodeQL are queued; predecessor receipts do not transfer. | Fresh successor-head parser/full/rendered regressions, CodeQL GREEN, security gates, qualifying independent approval, then normal parent-first integration. |
| Embedded-image ingestion structural parsing | #1115 `6545b5ff...` | Quoted-tag RED→source fix, multiline/edge coverage, and the LF-only provenance-offset RED→causal fix are present on protected-main ancestry. The CodeRabbit finding is resolved/outdated; exact-head Tests/Security/SAST/CodeQL remain queued and current Tests/CodeQL jobs are demonstrably pre-runner across `ubuntu-latest` and `ubuntu-24.04`. | Fresh exact-head repository/security/static-analysis GREEN and qualifying independent current-head approval before normal merge. |
| Dichotomous measurement-policy boundary | #902 `9487e029...` | Non-force convergence adopts #899's Proposed ADR lifecycle repair while preserving the governed measurement-policy delta; merge-base is exact and `behind_by=0`. Exact-head Tests are Draft-skipped, not GREEN. | Normal prerequisite integration, fresh repository/security/governance evidence and qualifying exact-head approval. |
| Dynamic-evaluation provenance projection | #915 `e2a00383...` | Ordinary two-parent convergence adopts current #902 while preserving the dynamic-evaluation delta and owner boundaries; merge-base is exact and `behind_by=0`. Exact-head Tests are Draft-skipped. | Fresh current-head required checks, released-owner integration evidence and qualifying approval after prerequisites. |
| Semantic operator/API naming continuation | #966 `f93715af...` / #919 `d78390c5...` | Both direct #899 descendants were converged non-force onto current `a2da5875...` with exact merge-base and `behind_by=0`; their existing bounded naming/operator deltas remain distinct and Tests are Draft-skipped. | Keep Draft; fresh exact-head hosted evidence and independent review after #899 integration/reconvergence. |
| Report-axis σ/share missingness | #866 `6935ac4d...` | Minimum `App.tsx` causal repair now consumes the four-state aggregate projection and omits null evidence. Tests `35049868360` ended `action_required` without jobs, so the source repair has no accepted repository GREEN. | Fresh frontend/full/rendered/a11y/i18n/security evidence and qualifying exact-head approval. |
| Comparison-graphic axis σ/share identity | #867 `09ee432b...` | Product/helper contract retained and the repaired #866 product head is non-force converged; no predecessor receipt transfers. | Fresh repository/rendered/security/review evidence on the current ancestry. |
| Singular/share tick stack | #868 `fbd637f1...` -> #875 `18448d39...` | Child deltas are preserved through ordinary non-force convergence on current #867; no acceptance evidence transfers. | Fresh exact-head repository/browser/security/performance/review evidence in parent order. |
| Report/comparison marker identity | #876 `6ef9d151...` -> #1033 `86ea83ba...` -> #1034 `bca51350...` | ζ/ζ/ξ owner boundaries are preserved on current ancestry; predecessor evidence is non-accepting. | Focused/full/rendered/security evidence and qualifying approvals. |
| Comparison origin/a11y identity | #877 `ddaa592a...` | Valid RED proves 8 comparison accessibility templates × 4 non-English SPA catalogs are absent. Existing single-writer automation is retargeted at `3750fc01...`; run `35053146771` is queued before steps. Product Tests `35051594411` are also queued. | Focused RED→GREEN source repair, fresh executable/rendered/a11y/security evidence and qualifying approval. |
| Catalog connection leases and summary TOCTOU | #1077 / #1080 | Separate owner lanes; external/model work must not hold long DB leases. | Causal RED→GREEN, short-transaction evidence and protected integration. |
| Governed UI translation delivery | #929 / #932 | Versioned translation-ledger work remains in its canonical owner lane. | `ko/en/ja/zh/vi/es/de/fr`, normal/loading/empty/error/permission/responsive, keyboard/focus/screen-reader, CJK expansion/font fallback. |
| MCP buyer-path latency | #1009 | Repository target remains p95 ≤ 20 ms where applicable. | Representative measurements and causal profiling; Rust-first hot-path repair if warranted. |
| Release identity and immutable publication | #961 / #1056 | Protected main is not release-ready. | One protected SHA with version/CHANGELOG/tag/package/release, SBOM/provenance, reproducibility and rollback evidence. |

## Evidence and ownership rules

Queued, skipped, COMMENTED, cancelled, rate-limited, status-only, predecessor-head or source-neutral results are not GREEN evidence for a moved head. A successful dispatcher or coordinator proves handoff only; producer execution and consumer settlement must complete on the same exact head.

A moved parent invalidates descendant ancestry immediately. Repair is ordinary/non-force: inspect intervening deltas, preserve valid product/test/fixture/contract evidence, then create a semantic two-parent convergence or safe reconstruction. Force-push, destructive rebase and receipt inheritance are not acceptable substitutes.

A conflicted or stale PR is repaired, not closed by convenience. Closure requires normal merge, complete verified successor inheritance, user direction, no valid delta, or another explicitly permitted condition.

Canonical domain truth stays with its owner. LineageWeave consumes released contracts/ACLs and does not copy contextual-orchestrator routing/admission, central CI queue policy, psychometrics implementations, ranking, scheduling or other owner functionality.

External/model work stays outside long-lived explicit database transactions and locks. Persistence reacquires the shortest necessary lease, revalidates authorization/version state, and uses idempotent/UPSERT semantics where required.

Material UI acceptance requires rendered buyer-path evidence in addition to unit/repository checks: normal/loading/empty/error/permission/responsive states, pointer/touch/keyboard/focus, accessibility, locale expansion/font fallback and applicable performance evidence.