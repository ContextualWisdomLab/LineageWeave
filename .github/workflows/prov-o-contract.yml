name: PROV-O contract

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft, closed]
    branches: [main]
    paths:
      - "lineageweave/prov_o.py"
      - "tests/test_prov_o.py"
      - "tests/test_prov_o_schema.py"
      - "tests/test_postgres_locale_contract.py"
      - "migrations/0017_prov_o_standard_relations.sql"
      - "docs/ontology/prov-o-support-profile.ttl"
      - "docker/postgres-init/Dockerfile"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/prov-o-contract.yml"
  push:
    branches: [main]
    paths:
      - "lineageweave/prov_o.py"
      - "tests/test_prov_o.py"
      - "tests/test_prov_o_schema.py"
      - "tests/test_postgres_locale_contract.py"
      - "migrations/0017_prov_o_standard_relations.sql"
      - "docs/ontology/prov-o-support-profile.ttl"
      - "docker/postgres-init/Dockerfile"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/prov-o-contract.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.repository }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  standards-contract:
    name: Registry, inference, coverage, PostgreSQL
    if: github.event_name != 'pull_request' || (github.event.action != 'closed' && github.event.pull_request.draft == false)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16.15-bookworm@sha256:bb3e1a57e5407e0a5280b4211980a5e537f4abd234a87014ac979849a78dd825
        env:
          POSTGRES_PASSWORD: postgres
          LANG: en_US.utf8
          POSTGRES_INITDB_ARGS: "--locale=en_US.utf8"
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      LINEAGEWEAVE_TEST_POSTGRES_ADMIN_DSN: postgresql://postgres:postgres@localhost:5432/postgres
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Set up locked Python dependency manager
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: "0.11.28"
          enable-cache: false

      - name: Install the committed universal lock
        run: uv sync --frozen --extra dev

      - name: Verify complete relation behavior and 100 percent coverage
        run: |
          uv run --frozen python -m coverage run --branch --source=lineageweave.prov_o \
            -m pytest -q tests/test_prov_o.py
          uv run --frozen python -m coverage report --fail-under=100 lineageweave/prov_o.py

      - name: Require a reachable PostgreSQL service
        run: |
          set -euo pipefail
          for _ in $(seq 1 30); do
            if pg_isready -h localhost -p 5432 -U postgres; then
              exit 0
            fi
            sleep 2
          done
          echo "PostgreSQL service is unreachable; the schema contract would silently skip." >&2
          exit 1

      - name: Verify PostgreSQL locale contract
        env:
          PGPASSWORD: postgres
        run: |
          set -euo pipefail
          locale_row="$(psql -h localhost -U postgres -d postgres -AtF '|' \
            -c 'select datcollate, datctype from pg_database where datname = current_database()')"
          test "$locale_row" = "en_US.utf8|en_US.utf8"

      - name: Verify normalized PostgreSQL contracts
        run: uv run --frozen python -m pytest -q tests/test_prov_o_schema.py tests/test_postgres_locale_contract.py

      - name: Compile owned Python surface
        run: uv run --frozen python -m compileall -q lineageweave/prov_o.py tests/test_prov_o.py tests/test_prov_o_schema.py tests/test_postgres_locale_contract.py
