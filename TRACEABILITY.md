# LineageWeave traceability

| Requirement | Implementation | Verification |
| --- | --- | --- |
| Direct PostgreSQL architecture | `LineageApplication.payload`, `build_source_query`, `persist_analysis_payload`, direct batch CLI/wrappers | PostgreSQL persistence and health contracts; normal CLI and wrapper runs keep JSON, analytics, and DOT export disabled unless an operator provides an explicit output path. Focused CLI/wrapper contracts pass. |
| Current direct-PostgreSQL aggregate integrity | `analysis_run_records`, report/score relations, transactional outbox | A bounded `READ ONLY` aggregate confirmed matching 43,814-row/43,707-document/42,467-thread run metadata; 80 reports with complete Judge fields; 290 linked scores across 58 report slices, 22 explicit unlinked slices, 15 finite calibration rows, and zero orphan candidate-evidence rows. No document body, image payload, or identifier was returned. |
| TEPP integration boundary | Separate direct-PostgreSQL product; TEPP and contextual-orchestrator exchange only through explicit import or HTTP; report psychometrics delegate to fast-mlsirm and fail closed without package output | PRD/literature reread; actual local fast-mlsirm connector and Rust-backed EAP smoke; missing/malformed/recorded connector bodies produce no score rows; unmerged upstream changes are not treated as integrated. A fresh read-only Compose inventory found no TEPP service URL or credential. |
| TEPP v1 analysis-run port | `tepp_http_config`, `normalize_tepp_analysis_request`, `post_tepp_analysis_run`, `get_tepp_analysis_run`, `/api/admin/tepp/analysis-runs`, `analysis_tepp_run_records` | Contract tests and a loopback wire smoke cover `POST accepted` → `GET completed`, version/unknown-field/size/idempotency/TLS/degraded-service handling, external lifecycle metadata, same-corp persistence, and the React administrator entry point. TEPP's current source and doctoring explicitly state that this is not a live HTTP server; missing configuration remains `tepp_service_unavailable`. No mock, loopback, or local service is used as scientific-runtime or release evidence. |
| TEPP research doctoring | `docs/doctoring/tepp-literature-review.md`, `notes/tepp_research_notes.md`, `OA_METHOD_PAPERS` | Full APA 7th TEPP register retained locally, including RAGAS; LayoutLM/LayoutLMv2/DocFormer/Donut and RAGAS OA originals and metadata are stored through Local Zotero with 13/13 attachment digests |
| Concurrent KG snapshot safety | `_lock_knowledge_graph_snapshot`, conditional legacy-column migration, production schema-lock release, MVCC-friendly node-before-edge `DELETE` replacement | Direct KG fixture, lock-release, payload/inspection contracts; fresh 308-pass 100% line-and-branch run |
| Cold document-list performance | `ensure_lineage_query_indexes` installs partial inferred/predicted source and target edge indexes before the first direct database list query | Aggregate-only direct-PostgreSQL plan reduced the representative one-item actor-filtered list query from 16.863 seconds to 1.062 seconds; cold server health, session, list, content, and queue probes passed |
| Browser identity acceptance | `web/e2e/login-gate.mjs`, `web/e2e/lineageweave.mjs`, `begin_keyverse_login`, `LineageHandler` | The local browser gate uses a synthetic address to verify email UX and unavailable configuration; a preauthenticated development actor is explicitly labeled `preauthenticated_session` and `reached_identity_authority=false`. Production Keyverse with a real business account must prove login, callback, session, and logout. |
| Full-corpus document search | `load_visible_document_index(..., search=...)`, `/api/documents?q=`, debounced React index request | Parameterized PostgreSQL search remains inside corp/PU ABAC; cached and cold-path contracts plus production React build |
| Integrated semantic document search | `semantic_search_documents`, `/api/documents/semantic-search`, `load_authorized_embedding_candidates`, `load_visible_document_index`, explicit React action | Query uses a verified live embedding transport and ranks only actor-authorized direct-PostgreSQL vectors. If no candidate clears the semantic threshold, the API returns explicit `keyword_fallback` matches from the same actor-filtered PostgreSQL index; it never turns them into Lineage. Focused API/ABAC contracts, the React build, and an isolated browser reader run that returned three authorized fallback matches pass. A configured real-account runtime search remains external acceptance evidence. |
| Search-result ranking and source entry | React semantic-result rail labels a semantic match with relatedness and labels `keyword_fallback` as title/document matching; each result enters the authorized source/timeline detail | Current MU-02 context, focused React surface contract, production build, and rebuilt direct-PostgreSQL product image. Ranking is never drawn as Event Lineage; a real-account configured-runtime search remains external acceptance evidence. |
| Search-result lineage integrity | `build_event_lineage` puts only observed same-document `row_successor` events in `event_lineage.beads`; inferred/predicted links, including `shared_thread_identifier`, are returned in `event_lineage.relatedness`. React renders connectors only for explicit observed row-successor markers and renders relatedness in a separate non-chronological panel. Reader detail/KG paths calculate, but do not persist, missing predicted relatedness. | Targeted shared-thread-versus-transition regression, browser payload/UI count contract, React production build, and current no-omit test run. The direct-PostgreSQL startup migration corrected only legacy shared-thread rows in both Lineage and KG projections; an aggregate after startup retained 107 observed same-document row-successor edges and 1,265 inferred shared-thread relations in each projection, with no legacy observed revision relation. |
| Evidence-gated event segmentation | `event_lineage.has_observed_transition`, `partitionLineageBeads`, `renderEventLineage`, `.lineage-segments`, `.lineage-observations` | A numbered horizontal segment contains only adjacent persisted observed `row_successor` beads. When part of a selected document lacks a transition, the connected segment and independent observations render separately; an aggregate/edge disagreement fails safe to independent observations. Corrected aggregate-only direct-PostgreSQL evidence found 78 multi-event documents, all 78 with a direct event transition, 107 persisted row-successor edges, and zero multi-event documents without one. Search ordering and relatedness cannot create a connector. |
| Keyverse OIDC boundary | `begin_keyverse_login`, `complete_keyverse_login`, `actor_for_request`, `LineageHandler`, `/api/login`, `web/e2e/login-gate.mjs` | The React gate validates a business-email format before its single `계속하기` action and exposes invalid input through `aria-invalid` and an alert. Product source has no account-provisioning, local email-capture, or browser-passkey challenge/attestation relay path; Keyverse owns account setup and passkey policy after redirect. The reproducible Playwright gate uses only a synthetic address and verifies one login action, no first-use or protocol labels, clear empty/invalid email errors, and a generic in-page `503` configuration result. `GET` and `POST` for both retired registration URLs, plus canonical and bare issuer-shaped discovery, authorization, token, and introspection routes, return `404` from both product and worker. The retained issuer-shaped test utility is audit material only and cannot supply browser-acceptance evidence. Real production Keyverse, a business account, callback, session, logout, and passkey acceptance remain required. |
| Authenticated identity disclosure | React `#sessionMeta` | The authenticated header names the current workspace and a Korean business-permission label; it does not render the Keyverse account subject. A fresh local browser login confirmed the raw account identifier was absent after callback. |
| Customer screen evidence boundary | `customer_surface`, `/api/customers`, actor-aware `load_customer_master`, React customer screen | Customer accounts and affiliate edges are returned only when their normalized account-to-document evidence survives document ABAC; source-document buttons reopen the authorized workspace. Customer-scope regression and HTTP contracts pass. |
| Customer-master hierarchy surface | `customer_surface`, `customerTreeRows`, normalized `parent`/`child` edges, accessible customer tree nodes | A searched subsidiary remains the first selectable result while the server adds only its persisted, actor-scoped ancestor accounts and their existing affiliate edges. The tree therefore retains its evidence-backed hierarchy depth without inventing a parent or crossing ABAC scope. The current direct-PostgreSQL aggregate found all 22 affiliate edges have both persisted account endpoints; focused API contract, React surface contract, and production build pass. |
| General-user business home | React `#userHome`, default `activeView`, reader-safe navigation to workspace and customer screen | Reader-surface contracts cover reader-safe navigation, document detail, source evidence, and Knowledge Graph without exposing administrator controls. The current Figma file contains an event-intelligence reference frame, but no matching user-chosen-browser capture exists at the same state; `design-qa.md` records the still-blocked parity decision. No local identity record supplies Keyverse acceptance. |
| Truthful initial loading state | React `documentLoadState`, `customerLoadState`, pending home metrics, customer count/list/detail/tree states, and report-load guard | The reader home and dedicated customer screen display pending/loading copy until actor-scoped PostgreSQL/API responses settle; an authorized empty result remains distinguishable from an in-flight request. React production build, surface contract, and browser data-bearing acceptance pass. |
| Retained issuer-shaped protocol material | Isolated source/test material retained for ownership audit; it is absent from the product Compose worker and image | It is not a supported local login test, Keyverse emulator, or release evidence and must not be started to manufacture acceptance. The product-only browser gate validates email UX without contacting an authority; the worker boundary guard and rejected issuer-shaped routes verify that product Compose remains issuer-free. Actual Keyverse business-account login/callback/session/logout remains mandatory external acceptance evidence. |
| Empty authorization-scope UX | React `documentLoadState`, `customerLoadState`, home cards, and document-list empty state | A settled reader scope with zero visible records must say that no authorized work or customer master is available; it must not remain on a loading message. The focused React surface contract and production build pass. The data-bearing E2E gate still rejects an empty scope when configured to require data. |
| Customer master ontology binding | `analysis_customer_accounts`, `analysis_customer_affiliates`, `analysis_customer_document_links`, `attach_customer_master_knowledge_graph`, normalized ontology/semantic tables | Customer nodes use `schema:Organization`; affiliate edges use `schema:subOrganization`; document evidence uses `schema:about`; actor-filtered evidence is required before UI/KG exposure. ADR-0001 customer-semantic amendment and customer-scope tests. |
| Administrator mode | `keyverse_admin_accounts`, `update_keyverse_account`, `/api/admin/keyverse/accounts`, React admin screen | Server requires verified `admin`, fixes target corp to the actor, filters cross-corp users, writes only reviewed account attributes and same-client roles, and never returns credentials. When the reviewed Keyverse Admin directory is not configured, the browser reports a user-facing availability state while ABAC/RBAC policy and Lineage review remain available; no local issuer or fake account is substituted. Keyverse Admin adapter, mutation, forbidden-route, and React contracts pass. |
| Administrator access-policy and Lineage review | `#accessPolicyScreen`, `#lineageReviewScreen`, `/api/admin/lineage/edges`, `/api/admin/lineage/edges/override`, `analysis_lineage_edge_overrides` | Same-corp admin-only ABAC/RBAC policy list; inferred/predicted exclude/restore is durable and outbox-audited; observed transitions are rejected; reader direct-route denial and API/React contracts pass. A separate administrator browser run reached both screens, loaded review candidates, and restored a relatedness visibility override after private/public HTTP 200 mutations. The browser E2E reads the API's `items` field, so its recorded review-candidate count cannot silently collapse to zero through an obsolete `edges` field. |
| Corp/PU are verified attributes, not credentials | `_actor_from_value`, Keyverse `org`/`workspace` account claims, no product password input | Contracts reject either missing account dimension before ABAC/RBAC. Actual production claim projection remains external acceptance evidence. |
| Same-company different-PU KG | `cross_pu_transaction`, `cross_pu_thread` | Cross-PU fixture plus a bounded current direct-PostgreSQL aggregate: four observed `cross_pu_transaction` edges. |
| Different-company KG | `cross_corp_transaction`, `cross_corp_thread` | Cross-company fixture supports the relation. The current corpus has zero multi-corp documents and zero multi-corp threads, so no cross-company edge is fabricated. |
| Same-PU different-company KG | `cross_corp_same_pu_transaction`, `cross_corp_same_pu_thread` | Cross-company same-PU fixture supports the relation. The current corpus has zero same-PU multi-corp threads, so no such edge is materialized without evidence. |
| Precomputed Keyman selection | `build_knowledge_graph`, Keyman buttons in `web/src/App.jsx`, `/api/documents/{document}/knowledge` | KG neighborhood tests and a fresh data-bearing browser contract that selects an actual LLM Keyman, opens the authorized KG, and checks relationship-direction output |
| Typed Keyman actors | `normalize_keyman_side`, `keyman_actor_name`, `build_knowledge_graph`, React Keyman editor | Institution actors are `organization`, meso units are `team`, person/org/team qualifiers and Node/Entity/Relationship/Direction fields survive persistence and KG traversal; legacy person syntax remains compatible. Institution/team regression and full test suite pass. A bounded live `gpt-4.1-mini` smoke returned three actors including organization/person with zero organization/team `person_name` coercions; no row was persisted. A fresh admin browser click test saved an explicit organization actor through the UI, verified HTTP 200/no coercion, and restored the original Keyman state. |
| Ontology and semantic layer in PostgreSQL | `semantic_layer_records`, `persist_knowledge_semantic_layer`, normalized namespace/term/rule/node/edge tables | `test_semantic_layer_normalizes_kg_terms_rules_and_authorized_context` |
| Agent answer grounded in semantic DB context | `LineageApplication.chat`, `load_knowledge_semantic_context` after actor-filtered KG selection | chat data-flow assertion verifies semantic context reaches the live-chat request |
| Customer-master KG evidence scope | `analysis_customer_document_links`, `filter_customer_master_for_documents`, customer nodes in `build_knowledge_graph` | `test_customer_master_semantics_require_document_evidence` |
| Korean summary, events, R&R, 글 자체의 Lineage | document node fields and native popup; PROV qualified attribution for person/organization agents; ORG Membership for supported affiliation; rank/title-qualified person identity | organization/person/same-name R&R direction test plus payload/UI contracts |
| Task-specific LLM enrichment | `post_keyman_http` for two-sided Keyman; `post_product_llm_http` for bounded subject-role classification, R&R, appointments, customer master, issue work, and report judge | role alias/abstention and product transport isolation tests, live-gateway request-shape tests, and a read-only one-record live adapter smoke; customer hierarchy requires returned document evidence |
| Product-task live fallback | `resolve_product_transport`, `compose_standin_transport`, Compose `/api/v1/product_task` | Direct gateway is preferred; when it is unset, the issuer-free Compose proxy forwards the full product-task allowlist to the configured live model gateway. Worker route, structured-output fallback, unsupported-task, resolver, and unavailable-path contracts pass; no recorded or synthetic result is returned. |
| LLM-derived customer commitments | `enrich_pending_appointment_records`, `analysis_appointment_records`, `publish_appointment_enrichment_events`, PostgreSQL outbox, Valkey Stream | Operator-only enrichment is capped at 64 documents, selects only appointment-hinted summaries with an explicit date, and sends title/summary text only through the existing live product transport. It replaces extracted rows only when every returned appointment is LLM-derived; otherwise it preserves the prior extraction. Each committed batch receives an internal marker and only matching committed rows are delivered, never a broad queue flush. A bounded 16-document live-HTTP batch produced 14 LLM rows and two preserved fallback cases; aggregate-only verification found 6,982 appointment rows across 6,982 documents (14 LLM, 6,968 extracted), 14 scoped events published, zero scoped events pending, and Valkey ready. |
| LLM-derived issue To Do/calendar work | `enrich_pending_issue_work_items`, strict due-date parser, nullable `analysis_calendar_items.occurred_on`, `publish_issue_work_enrichment_events`, PostgreSQL outbox, Valkey Stream | Only a full explicit calendar date is persisted; missing or invalid dates stay unscheduled and the React popup says `일정 미정`. A no-user-identity batch is capped at 64 pending tickets and sends only title plus a capped Korean summary. It upserts only a complete LLM To Do/calendar pair and delivers only events carrying its exact internal batch marker. One live-HTTP 8-ticket batch completed 3 pairs, retained 5 fallback rows, and published 3/3 scoped events. Aggregate-only verification then found 5 LLM and 28,207 pending rows in each work relation; all 28,207 pending calendars were unscheduled, while 3 of the 5 LLM calendars were intentionally unscheduled because no explicit date was supplied. |
| Paper-grounded adaptive orchestration | `build_orchestration_envelope`, `_orchestration_request_fields`, task-aware `_post_chat_completion_json`, multimodal inspection and lineage-chat transport | Simple tasks select single-model routing; enrichment, report, verification, and image tasks select bounded deep composition with explicit access list, recursion, and reasoning effort. Direct gateways receive portable nested metadata; only contextual-orchestrator receives top-level route/conduct controls. Focused HTTP-shape tests and the current 355-test/100%-coverage run pass; live reader/admin LLM browser workflows pass; upstream multimodal support remains review-gated. |
| Administrator-bounded LLM enrichment | `POST /api/admin/enrichment/run`, `GET /api/admin/enrichment/status`, `_enrichment_candidates`, PostgreSQL outbox, normalized work rows | 64-document cap and task validation contracts; direct PostgreSQL administrator smoke queued one Keyman document and completed 1/1 with 0 failures and 1 explicit abstention; no document content is returned by status |
| 3NF issue-ticket parent integrity | `analysis_issue_tickets`, `persist_issue_work_items`, `persist_operational_surfaces` | Automatic To Do/calendar paths upsert a ticket parent before work rows. Full snapshots reconcile only pipeline-created parents. A metadata-only direct-PostgreSQL repair inserted 28,211 missing parents, preserved two non-automatic tickets, and left zero work items without a ticket parent. |
| Authorized issue-ticket lifecycle | shared `ticket_status` enum, document-scoped `manage_tickets`, direct PostgreSQL ticket/To Do updates, transactional outbox | `open`, `in_progress`, and `resolved` are validated before mutation. The same document authorization decision scopes ticket lookup and both updates; the existing `issue_ticket_changed` outbox event is emitted and cached detail state is kept consistent. Focused application/HTTP/UI contracts cover allowed, invalid, denied, missing-ticket, cache-miss, and Valkey paths. |
| Source citations and AJAX drawer | evidence route and chat evidence IDs | `source_evidence`, React source route contract, and a bounded selected-browser live-chat run against direct PostgreSQL: one non-empty answer returned five citations, including one VOC citation whose click opened the authenticated source drawer; no raw source content or identifiers were recorded. |
| 40MB-plus inline content | byte length/prefix plus database-side image/markup marker, manifest, authorized asset endpoint, 50 MiB authorized-model boundary | Marker/classifier, asset-boundary, and persisted-eligibility re-evaluation tests keep a 40 MiB encoded raster eligible without returning source bytes in graph or browser payloads. |
| No graph byte/base64 export | `public_asset_metadata`, persisted KG metadata | content manifest implementation and ADR success criterion |
| Bounded inline-image inspection | `prepare_content_inspection_asset`, document-local asset index, inspect route | Strict-raster, route, and persisted-metadata regression tests enforce the 50 MiB request ceiling. Seven earlier aggregate-only live inspections remain evidence; no inspection was repeated for this policy change, and a gateway rejection never fabricates OCR. |
| OCR/object 3NF persistence | inspection parent, label catalog, description on inspection-label relation | `test_content_inspection_keeps_label_descriptions_per_asset_relation` |
| Evidence-bound semantic embedding relatedness | `lineageweave_embeddings.py`, idempotent DOM content persistence, `index_document_embeddings`, `semantic_related_documents`, manager-only index route, reader retrieval route | focused chunk/transport/temporary-schema direct-PostgreSQL/3NF/ranking/authorization/HTTP contracts and 100% line-and-branch coverage; unchanged content reads preserve indexed rows while changed content invalidates them through the block foreign key. A live labeled multilingual query retained its intended document at 0.440, the detail content read retained all 29 chunks, and the bounded relatedness route returned in 0.19 seconds. Larger labeled calibration and real-Keyverse route acceptance remain release evidence |
| Verified model transport | `verified_gateway_ssl_context`, optional CA bundle | TLS request-shape test and one live HTTPS content-inspection run with a recorded model field |
| Scoped OCR search | `image_search`, filtered document set | image-search route and ABAC implementation |
| Docker Compose proxy without an IdP | `ensure_compose_standin`, event/inspection worker endpoints | The rebuilt direct-PostgreSQL product image reached database health; the current built-image boundary guard passes and all eight retired-registration/issuer-shaped probes return 404. The retained utility is neither copied into nor served by the product worker; its ownership disposition remains open. No local issuer is used for product or release acceptance. |
| Shipped product-runtime coverage | `lineageweave.py`, `lineageweave_embeddings.py`, `lineageweave_server.py`, `compose/http_standin.py`, retained issuer audit utility | A fresh current-tree run passed 355 tests with no skip. The explicit full-source report covers 7,627 statements and 2,984 branches at 100% line-and-branch coverage. The retained utility remains audit-only and is not product identity behavior. |
| React presentation-model coverage | `web/src/ui-model.js`, `web/src/ui-model.test.js`, `web/vitest.config.js` | Vitest V8 covers the pure email-validation, event-lineage segmentation, Keyman, directed-KG relationship presentation, asset-preview, semantic-value, and customer-tree model at 100%: 103 statements, 196 branches, 28 functions, and 88 lines. The gate runs before the React build in the scheduled verifier and container web build. Generated coverage/build output is excluded from version control and the container build context so local paths cannot become public artifacts. This does not mislabel the remaining component interaction surface as unit-covered; it remains covered by the browser contracts/E2E. |
| Browser-cancelled response handling | `LineageHandler._send` | A cancelled response catches `BrokenPipeError`/`ConnectionResetError` without attempting a second write; the HTTP contract regression and production server compilation pass. |
| Retained issuer-utility audit | `compose/keyverse_oidc.py` | The complete source gate covers the non-shipped utility as an ownership/audit artifact. It is not product identity behavior, an approved identity authority, or release evidence. |
| Model rate-limit safety | `_post_chat_completion_json`, `post_keyman_http`, `derive_dichotomous_judge_via_llm`, `persist_period_reports`, `common_enum_values` | A live full-source run encountered gateway 429 responses without aborting: 80 reports persisted with non-null `abstain` verdicts, 12 Zotero parent/original pairs remained stored, and the runtime schema contract passed. Non-rate-limit failures retain their existing error boundary. |
| Business-account browser acceptance | `web/e2e/login-gate.mjs` | The current browser check covers email-first validation and unavailable-configuration guidance only; it starts, contacts, and represents no identity authority. A configured production Keyverse run with a real business account must prove login, callback, session, and logout before release. |
| Method-paper research provenance | `store_oa_method_paper`, exact parent/attachment reuse, metadata-refresh preservation, `zotero_connector_attachment_url`, `analysis_method_paper_records` | Current Local Zotero run stores 13 parents and 13 bounded originals with non-empty SHA-256 digests, including four multimodal document-analysis papers and the RAGAS evaluator; repeat lookup requires exact title/source URL and attachment digest before reuse |
| Cloud-native product run path | Root `Dockerfile`, `product` Compose profile, direct-DB environment interpolation, optional operator env-file injection, `preflight_product_compose.sh`, Compose-network worker URL, host-gateway direct PostgreSQL mapping | The profile forwards only operator-provided direct-DB values and never commits them. A rebuilt Compose-managed direct-DB product reached healthy status at the active local origin; its browser email-entry check and both `GET`/`POST` retired-registration plus all four issuer-shaped route rejections passed. |
| Live model boundary without fabricated fallback | Compose forwards to configured gateway; missing gateway returns unavailable | `compose/http_standin.py`, worker contract tests |
| Keyman Compose fallback | `resolve_keyman_transport`, `ensure_compose_standin`, `compose_standin_transport` | Direct gateway is preferred; when absent, the Compose live-model proxy is started or reused. Resolver and unavailable-path tests prove no recorded response or fake actor is returned. |
| Adaptive KG depth | Per-node `kg_depth`, edge-cost traversal, optional API ceiling | `test_knowledge_neighborhood_uses_node_specific_depths`, `/knowledge?depth=` |
| Directed Keyman-KG presentation | `knowledgeEdgeRows`, `#popupKnowledgeEdges`, persisted `source`/`target`/`relation`/`evidence_status` | The Keyman popup renders every valid edge in the server-bounded neighborhood as `source → target`, with entity types, a customer-readable relation label, the unmodified relation code, and evidence status. Missing endpoints or relations fail closed rather than rendering orphan identifiers. Vitest V8 is 100%, the React surface contract passes, and the production bundle builds. |
| Valkey event queue | Transactional PostgreSQL outbox flushed to `lineageweave_events` Stream | `analysis_event_outbox`, `publish_valkey_event`, `/api/queue/health`; bounded appointment delivery selects only the committed batch marker and cannot flush unrelated pending events |
| Authenticated report detail and operator queue state | Reader and administrator report drilldown plus the authenticated Event Queue KPI reuse existing `/api/reports` and `/api/queue/health` contracts; report detail renders four RAGAS Judge metrics with evidence-document links | React surface contract and production build; fresh data-bearing browser run opened a report with 4 metric cards and 32 authorized evidence links; report-linked document selection remains server-authorized |
| Evidence-backed reader report labels | `load_report_document_nodes`, `load_authorized_report_document_numbers`, `attach_report_display_labels`, `reportBusinessTitle` | PU/team labels use authorized business attributes; project labels use the first actor-authorized document title or Korean summary and never the opaque project slice key. Missing evidence remains a generic scope label. The response-only label is not persisted as an Ontology/Semantic Layer fact or Lineage edge; focused API/React contracts and full 100% Python coverage pass. |
| Temporal psychometric linking | `score_period_reports` sends `report_id` as the observation group, attaches only package-produced linked scores back to the same report, preserves `slice_key` for display, and marks missing output `unavailable` | Two-window same-PU regression; live PostgreSQL reanalysis produced 80 LLM-judged reports, 15 finite item calibrations, and 290 `fast_mlsirm` scores across 58 report slices; 22 slices remain explicitly unlinked; connector-error and recorded-body regressions produce empty score lists |
| Evidence-bound factor-item catalog | `derive_factor_item_catalog_via_llm`, `parse_factor_item_catalog`, `analysis_factor_items`, `analysis_factor_item_evidence`, `analysis_factor_item_calibrations` | Live LLM catalog derived five candidates from supplied writings and persisted ten evidence-bound links. Ten fixed anchors plus five candidates all reached `calibrated` only after the separate Rust-backed connector returned finite output; candidate rows without allowlisted evidence are discarded. |
| RAGAS-aligned report evaluation | `DEFAULT_EVALUATION_METRICS`, `parse_ragas_metric_scores`, `analysis_evaluation_metrics`, `analysis_report_metric_scores`, `analysis_report_metric_evidence`, `persist_period_reports`, `load_period_reports` | Live LLM re-evaluation completed 80/80 report slices with 320 metric rows: 80 each for faithfulness, answer relevancy, context precision, and context recall; all rows have `llm_judge` provenance, scores in `[0,1]`, and zero invalid scores. Evidence references are normalized into 2,440 child rows with zero orphans. The current 290 package-produced linked scores and 15 calibration rows remain separate. Missing reference evidence is represented as `abstain`/NULL rather than a fabricated zero. |
| Rust longitudinal report state | Report period provenance is sent through the separate fast-mlsirm connector; state specifications, run diagnostics, and occasion estimates persist in `analysis_longitudinal_state_specs`, `analysis_longitudinal_state_runs`, and `analysis_longitudinal_state_observations` | Connector smoke computed two real report occasions with `rust_cpu_multithreaded`, zero RMSE, exact sequence/date ordering; persisted reanalysis recorded 80 existing LLM-Judge observations across 40 groups with zero transitions; malformed state payloads fail closed; full joint estimator/recovery remains an upstream release gate |
| Independent Judge recovery | `refresh_persisted_reports` holds one advisory lock, selects only stale report IDs, verifies the rebuilt slice set, and merges refreshed records back with already-valid records; Compose caps maintenance at three slices, one attempt per slice, and a 15-second report-judge deadline | A bounded direct-PostgreSQL Compose recovery retained valid reports while retrying stale slices. The current aggregate is 80 of 80 `llm_judge` decisions, 290 `fast_mlsirm` scores across 58 slices, 22 explicit unlinked slices, and zero orphan scores. |
| Administrator report retry | `POST /api/admin/reports/refresh`, `LineageApplication.refresh_reports`, `#reportQualityScreen`, `#refreshReportsBtn` | Server-enforced admin action reuses the stale-slice advisory lock, reloads the authorized report surface, and emits `period_report_refresh_completed` through the PostgreSQL outbox. HTTP, React surface, and administrator browser contracts cover the action; unavailable providers remain unchanged/abstain. |
| Report-score referential integrity | `persist_period_reports` deletes score rows whose report no longer exists after window reconciliation | Red-green persistence contract plus live PostgreSQL recheck: 80 reports, 290 scores, 58 distinct score reports, 22 explicit unlinked reports, zero orphan scores |
| Fresh direct snapshot | Server reads runtime source on first process load; persisted tables are audit/cache, not source authority | `LineageApplication.payload` |
| Common enum table | `common_enum_values` | `test_common_enum_table_is_two_word_snake_case` |
| Product PostgreSQL object naming | Product-owned `analysis_`, `common_`, `semantic_`, `ontology_`, and `lineage_` relations | A bounded `BEGIN READ ONLY` catalog check covered 41 product-owned relations and found zero identifiers that violate the two-word snake_case rule. |
| Observed/inferred separation | `make_lineage_edge`, `analysis_lineage_edges.reason`, and evidence tier on every KG edge snapshot | lineage tests, restart-safe graph rehydration test, and ADR-0001/ADR-0004 |
| Evidence-verified ontology inference Agent | `verify_lineage_inferences`, `resolve_organization_alias`, bounded internal/external evidence, live LLM verdict, incremental KG upsert, verified-alias carry-forward in both full snapshot writers, and normalized `analysis_inference_*` relations | failure-closure and privacy contracts plus a live SearXNG-backed, externally cited `verified` alias decision persisted as inferred SKOS exact-match semantics and survived direct KG replacement and complete source reanalysis; ADR-0004 and ADR-0001 amendment |
| Central coverage-evidence recovery | Separate central-workflow PR sets a fixed non-caller-controlled request identifier on the existing trusted archive opener while retaining its fixed origin, no-proxy/no-redirect, size, and digest boundary | Focused boundary tests, a live pinned-digest check, and the full central test suite pass locally. The draft PR is still queued for CI and independent review; no job was retried. Keyverse PR #100 has no unresolved current review thread but retains its earlier changes-requested decision and lacks independent approval, so it must be independently reviewed after the prerequisite merges. |
| Governance and rollback | ADR-0001 through ADR-0006, `AGENTS.md`, `ARCHITECTURE.md`, `CHANGELOG.md` | ADR validator, public-content scan, boundary contracts, and source-hash coverage pass. The current Figma file contains an event-intelligence target, but product/Figma parity is blocked pending a matching user-chosen-browser capture at the same state. Production Keyverse, retained-artifact disposition, and independent repository approval remain external release gates. |

| General-user product surface | `web/src/App.jsx` `업무 홈`/`업무공간`/`고객 화면`, actor-filtered document/customer/report routes | Reader browser run reached the three business screens, report detail, source drawer, and Knowledge Graph with direct PostgreSQL data; administrator navigation and diagnostic KPI were absent. Administrator policy, Lineage review, enrichment, TEPP, and Keyverse-account controls remain separate and server-gated. |
| Stale shared-thread response guard | `is_current_shared_thread_relation`, persisted Lineage/KG/detail/review loaders, `tests/test_postgres_and_valkey_contract.py` | A persisted `shared_thread_identifier` edge is returned only when both current document endpoints still carry the stored thread identifier; stale history is retained for audit but cannot become reader Lineage, administrator review, or a Keyman neighborhood. Focused PostgreSQL contracts pass. |
| Customer snapshot truth and reader vocabulary | `persist_operational_surfaces`, `web/src/App.jsx`, `tests/test_postgres_and_valkey_contract.py` | Explicit customer-master snapshots clear document links, affiliate facts, and accounts child-first before replacement, including LLM abstention; a payload without the boundary leaves the projection intact. Reader screens show `근거 연결`, `공개`, and `내부` rather than raw implementation provenance. |
| Current shared-thread aggregate | direct PostgreSQL Lineage and KG projections, `is_current_shared_thread_relation` | Aggregate-only recheck found one retained stale audit row in each projection. It is excluded from current responses; 3,020 matching shared-thread pairs remain in each projection as non-temporal relatedness, and 107 observed `row_successor` edges remain the sole chronological links. |
| Executable Keyverse RP boundary | `OIDC_ISSUER_ROUTE_PARTS`, `LineageHandler`, Compose worker, HTTP contracts | Fresh rebuilt-runtime probe returned `404` for every product-and-worker `GET`/`POST` discovery, authorization, token, and introspection route, including `/oidc/*` aliases. Anonymous access remains Keyverse-gated; no issuer was started or used. |
| Current product-runtime coverage | shipped Python runtime sources and `web/` V8 presentation model | 355 Python tests and 7,627 statements / 2,984 branches at 100% line-and-branch coverage; 8 React tests, V8 100%, and production build passed. This does not replace real-Keyverse or Figma/browser acceptance. |

| Container and CI supply-chain boundary | `Dockerfile`, `compose/Dockerfile`, `compose/searxng/Dockerfile`, `tests/Dockerfile.oidc-conformance`, `.github/workflows/tests.yml`, `.github/workflows/hourly-product-gap.yml` | Base images are immutable digest references; the product web-build stage and runtime images declare non-root users; workflow `uv` installation uses `--require-hashes` with the known Linux wheel digest. Container/workflow contract tests pass, and current Scorecard/Trivy checks are clean. |

| Reader Keyman vocabulary | `keymanSourceLabel`, `keymanStatusLabel`, `#popupKeymanOur`, `#popupKeymanCounterpart` | The general-user popup hides raw `source`/`status` model codes and renders business labels for automatic derivation, user management, pending, and unavailable states. Raw provenance remains available to authorized server/admin review; the actor-filtered Ontology/Semantic Layer and Keyman KG behavior is unchanged. Static surface contract and production build pass. |

| Reader report and customer vocabulary | `reportBusinessTitle`, `reportVerdictLabel`, `reportLinkingLabel`, `reportJudgeSourceLabel`, `customerTierLabel`, `#userHome`, `#periodReports`, `#reportDetail`, `#customerScreen` | General users see Korean report period/scope/verdict, score-linking, Judge-source, and customer hierarchy terms instead of generated slice IDs, `pass`/`fail`, `fipc`/`LLM_JUDGE`, or `group`/`hq`/`plant` storage codes. The normalized API and administrator audit boundary retain raw values; static surface, direct-data browser captures, and production-build contracts cover the presentation rule. |

| Hourly product-gap proposal loop | .github/workflows/hourly-product-gap.yml, docs/operations/hourly-product-gap-loop.md | Workflow contract tests require the minute-17 schedule, NIM-only model boundary, immutable action pins, bounded patch artifact, fresh Python/React verifier, queue/base recheck, and PR creation without merge authority. The local product has no remote, so a live scheduled run remains a deployment gate. |

## Acceptance commands

```bash
uv run --group dev python -m pytest -q
uv run --group dev python -m py_compile lineageweave.py lineageweave_embeddings.py lineageweave_server.py compose/http_standin.py
uv run --group dev coverage run --branch --include='lineageweave.py,lineageweave_embeddings.py,lineageweave_server.py,compose/http_standin.py,compose/keyverse_oidc.py' -m pytest -q
uv run --group dev coverage report --fail-under=100
cd web && npm run coverage && npm run build
```
