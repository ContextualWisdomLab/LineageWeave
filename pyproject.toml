[project]
name = "lineageweave"
version = "0.2.19"
requires-python = ">=3.10"
dependencies = [
    "psycopg[binary]>=3.2",
    "truststore>=0.10.4",
]

[dependency-groups]
dev = [
    "coverage>=7.6",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
